# 餐厅收银打单应用 — 开发计划（第一步）

> 项目：`big_boss_bro` · 目标平台：安卓 + Windows · 语言：中文优先（后续西语/英语）

## 背景

做一款类似 Loyverse 的餐厅收银应用：点单 dashboard → 客人付钱 → 蓝牙打印机打小票 → 订单历史管理（含删除）。最终在**安卓 + Windows** 两个平台跑。语言先做**中文**，之后加**西语和英语**。参考 UI 在 `assets/` 里的 Loyverse 截图。

**第一步只做：安卓上的「界面 + 点单 + 蓝牙打印机打单」**。下面这份计划就是这第一步，以及后续如何扩展。

## 技术选型（简单解释）

- **框架：Flutter**。同一套代码同时编译出安卓版和 Windows 版，最省事。像搭积木一样写界面，适合新手，不需要会安卓原生开发。
- **蓝牙打印：`esc_pos_bluetooth` 包**。Flutter 生态里对小票热敏打印机支持最成熟的方案。
- **中文小票的关键**：中文热敏打印机用 GBK 编码（非 UTF-8），用专门编码库处理，避免乱码。
- **开发方式**：WSL2 里装 Flutter → 编译安卓安装包 → 通过手机**无线调试**装到一加 12 上测试。

## 里程碑 0：开发环境搭建（一次性）

1. WSL2 安装 **Flutter**。
2. 安装 **Java 17** + **安卓 SDK**（编译 APK 必需）。
3. 一加 12：开发者选项 → 开「USB 调试」+「无线调试」，手机和电脑连同一 Wi-Fi。
4. `adb pair` 配对，之后 `flutter run` 直接装机。
5. **验证点**：跑通「你好世界」页面。

## 里程碑 1：界面 + 点单（中文界面）

1. 项目目录创建 Flutter 工程（应用标签「收银台」）。
2. 项目结构：
   ```
   lib/
     main.dart                 # 程序入口
     app.dart                  # 主题（Loyverse绿 #1FA85A）、语言（中文）
     models/                   # 菜品/购物车条目/订单
     data/                     # 示例菜单 + 设置存取
     state/pos_controller.dart # 核心状态：购物车、点单、结账
     services/                 # 蓝牙打印服务 + 小票排版
     screens/pos_screen.dart   # 主点单界面
     widgets/                  # 分类标签/菜品格子/订单面板/付款弹窗/打印机设置
     l10n/app_zh.arb           # 所有中文字符串（以后加西语英语文件）
   ```
3. **主界面（Loyverse 风格）**：横排分类标签（热菜/主食/饮料…）→ 菜品格子（emoji + 菜名 + 价格，点击加购）→ 订单面板（菜名、−/+ 数量、小计）→ 底部绿色「结账」大按钮。
4. **示例菜单**先写死几条数据（牛肉炒饭 ¥28、可乐 ¥5…），之后自己改。
5. **点单逻辑**：点菜加购、重复点数量+1、改数量、删条目、清空。

## 里程碑 2：结账 + 蓝牙打单

1. **打印服务做成接口**：安卓用蓝牙实现；Windows 版以后换 USB 实现即可。
2. **小票内容**：店名 → 单号/时间 → 每行「菜名 数量×单价 金额」→ 小计/总计 → 支付方式 → 谢谢光临 → 切纸。58mm 每行约 32 字符（中文字算 2 格），自动对齐。
3. **打印机设置弹窗**：扫描蓝牙打印机 → 选中记住；可切换纸宽 58/80mm、改店名、改货币符号（自定义，默认 ¥）、「打印测试页」先验证。
4. **结账流程**：选支付方式（现金/刷卡/扫码）→ 打印 → 成功清空购物车并记入已结订单。**打印失败则弹「重试 / 跳过并收款」**，绝不丢单。

## 之后逐步做（不在第一步）

- **Windows 版** + USB 打单
- **订单历史管理**：本地数据库存已结订单，列表 + 删除
- **西语、英语**：加语言文件即可（已铺好路）
- 菜品/分类管理界面、菜品照片、折扣、桌号、税等

## 验证方式

- **单元测试**：金额计算、小票排版对齐（含中文宽度）、点单逻辑——用假打印机。
- **真机验证**：点几个菜 → 结账 → 小票打出、中文不乱码、金额正确 → 购物车清空。另测「打印机没连上时结账」不丢单。

## 关键风险与对策

| 风险 | 对策 |
|---|---|
| 中文小票乱码 | GBK 编码 + 「打印测试页」第一时间验证 |
| 安卓扫描不到蓝牙打印机 | 清单加全蓝牙权限 + 运行时申请 |
| 无线调试连不上 | 同一 Wi-Fi，必要时重新配对 |
| 打印机型号/纸宽未知 | 纸宽做成设置项（默认58mm），先打测试页 |
